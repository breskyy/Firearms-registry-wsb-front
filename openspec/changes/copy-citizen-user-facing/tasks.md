## 1. Citizen copy and diacritics

- [x] 1.1 Fix diacritics on `ApplicationCorrection` and `ApplicationsList`.
- [x] 1.2 Replace WPA with „urząd” in citizen-facing components and pages.
- [x] 1.3 Update `PromiseQrModal` (kod z QR, not token).

## 2. Citizen error messages

- [x] 2.1 Route transfer and correction errors through `getApiErrorMessage` / `translateTransferError`.
- [x] 2.2 Polish fallback in `translateTransferError` for unmapped API messages.

## 4. Remaining citizen copy

- [x] 4.1 Replace „sloty” with „miejsca w pozwoleniu” on citizen surfaces.
- [x] 4.2 Complete diacritics on `ApplicationCorrection`.
- [x] 4.3 Route promise/permit form errors through `getApiErrorMessage`.
- [x] 4.4 User-friendly attachment format labels; `promiseQrAvailability` urząd wording.
- [x] 4.5 Sync dead `ShopVerification` copy with shop terminology.

## 5. Verification

- [x] 5.1 Build passes.
