## 1. Shop dashboard and sale flow copy

- [x] 1.1 Rewrite `ShopDashboard` procedural and operational text without backend/dev terms.
- [x] 1.2 Rewrite `ShopSalePage` labels, tabs, placeholders, and validation messages (Token → kod z QR).
- [x] 1.3 Show Polish permit type labels in verification summary.

## 2. Scanner and error messages

- [x] 2.1 Update `QrScanner`, `cameraErrors`, and `cameraAvailability` user messages.
- [x] 2.2 Update `shopService.translateVerifyMessage` vocabulary and fallback.
- [x] 2.3 Replace developer network/API error text in `apiErrors` and `api.ts`.

## 3. Verification

- [x] 3.1 Manual pass: shop dashboard → sale → scan/manual code → submit success/error paths.
